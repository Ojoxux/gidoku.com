{
  "name": "basic",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client && vite build",
    "preview": "wrangler dev",
    "deploy": "pnpm run build && wrangler deploy",
    "db:setup": "tsx scripts/db-setup.ts",
    "db:seed": "tsx scripts/db-seed.ts",
    "db:migrate:local": "wrangler d1 execute gidoku-db --local --file=migrations/0001_initial_schema.sql && wrangler d1 execute gidoku-db --local --file=migrations/0002_add_sessions.sql",
    "db:migrate:prod": "wrangler d1 execute gidoku-db --file=migrations/0001_initial_schema.sql && wrangler d1 execute gidoku-db --file=migrations/0002_add_sessions.sql"
  },
  "private": true,
  "dependencies": {
    "hono": "^4.10.7",
    "honox": "^0.1.51"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "@hono/vite-build": "^1.3.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@tailwindcss/vite": "^4.0.9",
    "tailwindcss": "^4.0.9",
    "tsx": "^4.19.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
